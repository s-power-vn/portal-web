import { FC } from 'react';

import { BusinessFormProps } from '@storeo/theme';

export type NewColumnFormProps = BusinessFormProps;

export const NewColumnForm: FC<NewColumnFormProps> = props => {
  return <></>;
};
