version: "3.9"
services:
  spower-web:
    platform: linux/amd64
    container_name: "spower-web"
    hostname: "spower-web"
    image: "minhdtb/spower-web:1.0"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4200:4200"
